// Types the Compose compiler should treat as stable without annotating them in place.
// Keeping this out of the source lets the domain layer stay free of androidx imports.
//
// Every model here is an immutable data class: instances are only ever produced through
// constructors and copy(), and their collections are never mutated after construction.
// Verify with build/compose_reports/*-classes.txt after a build.
com.nikpapajohn.moviedb.domain.model.*
